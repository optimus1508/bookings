{{template "base" .}} {{define "content"}} {{$res := index .Data "reservation"}}
<div class="container">
    <div class="row">
        <h1 class="mt-5">Reservation summary</h1>

        <hr>

        <table class="table table-striped">
            <thead></thead>
            <tbody>
                <tr>
                    <td>Full name :</td>
                    <td>{{$res.FirstName}} {{$res.LastName}}</td>
                </tr>
                <tr>
                    <td>Arrivée :</td>
                    <td></td>
                </tr>
                <tr>
                    <td>Départ :</td>
                    <td></td>
                </tr>
                <tr>
                    <td>Email :</td>
                    <td>{{$res.Email}}</td>
                </tr>
                <tr>
                    <td>Phone :</td>
                    <td>{{$res.Phone}}</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
{{end}}